# 1. ממי אנחנו יורשים? תביא לי לינוקס שכבר מותקן עליו פייתון 3.9
# (גרסת slim היא גרסה רזה וקלה יותר)
FROM python:3.9-slim

# 2. איפה נעבוד בתוך הקופסה? צור תיקייה בשם app ותכנס אליה
WORKDIR /app

# 3. קודם כל תעתיק את רשימת הדרישות (כדי לנצל את ה-Cache של דוקר)
COPY requirements.txt .

# 4. תתקין את הספריות בתוך הקופסה
RUN pip install -r requirements.txt

# 5. עכשיו תעתיק את שאר הקוד שלנו (את קבצי הפייתון)
COPY . .

# 6. מה הפקודה שתרוץ כשמישהו יפעיל את הקונטיינר הזה?
CMD ["python", "calc.py"]